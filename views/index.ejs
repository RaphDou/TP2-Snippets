<%- include('includes/head.ejs') %>

    <body>
        <header>
            <%- include('includes/navigation.ejs') %>
        </header>

        <main class="container mt-5">
            <% if (articles.length> 0) { %>
                <h1 class="mb-4">Mes snippets</h1>
                <div class="row row-cols-1 row-cols-md-2 g-4">
                    <% for (let article of articles) { %>
                        <article class="col">
                            <div class="card h-100">
                                <div class="card-body">
                                    <h2 class="card-title">
                                        <%= article.title %>
                                    </h2>
                                    <p class="card-text">
                                        <%= article.tags %>
                                    </p>
                                </div>
                                <div class="card-footer">
                                    <div class="btn-group" role="group" aria-label="Actions">
                                        <a href="/article/<%= article.id %>" class="btn btn-success"><i
                                                class="bi bi-eye"></i> Voir</a>
                                        <a href="/edit-article/<%= article.id %>" class="btn btn-warning"><i
                                                class="bi bi-pencil"></i> Editer</a>
                                        <a href="/delete/<%= article.id %>" class="btn btn-danger"><i
                                                class="bi bi-trash"></i> Supprimer</a>
                                    </div>
                                </div>
                            </div>
                        </article>
                        <% } %>
                </div>
                <% } else { %>
                    <main class="container mt-5">
                        <div class="row justify-content-center">
                            <div class="col-md-6 text-center">
                                <h1 class="display-4">:&#40;</h1>
                                <p class="lead">Il ne semble pas avoir de snippets pour l'instant!</p>
                                <a href="add-article" class="btn btn-primary"><i class="bi bi-house-door"></i>Je veux créer un snippets!</a>
                            </div>
                        </div>
                    </main>
                    <% } %>
        </main>
        <%- include('includes/end.ejs') %>
    </body>